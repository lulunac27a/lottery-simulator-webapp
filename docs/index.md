# Lottery Simulator Webapp

This is a simple webapp that simulates a lottery game. The user can enter the number of balls to draw and possible numbers. The app then generates a random number for each ticket and displays the results.